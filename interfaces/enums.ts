export enum ChartType {
  Pie = 'Pie',
  Line = 'Line',
  Bar = 'Bar'
}

export enum DatePickerType {
  year = 'year',
  date = 'date',
  dates = 'dates',
  daterange = 'daterange',
}

export enum DatePickerSize {
  default = 'default',
  large = 'large',
  small = 'small',
}
